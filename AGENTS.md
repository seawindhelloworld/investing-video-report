# 项目工作约定

## 项目边界

- 本项目只从已完成勘误和质量验证的标准字幕包生成观点报告。
- 不下载视频、不转码、不执行 ASR、不修改字幕，也不清理上游文件。
- `work/{video_id}/transcript/transcript.corrected.jsonl` 是作者内容的唯一正文依据；原始 ASR、勘误记录和质量结果只用于追溯与风险提示。
- 仅凭字幕无法回听确认。未解决词项必须保留不确定性，不得声称已经用音视频证实。

## 内容边界

- 报告固定采用：`第一部分｜视频 / 作者内容` → `第二部分｜外部证据研判` → `第三部分｜Agent 综合判断`。
- 第一部分使用内容直述，不反复写“视频称”“作者表示”等主持式归因；实际说话人切换时保留必要归属。
- 讲者个人判断、持仓和交易行动使用醒目的“报告标注 · 视频内个人判断”卡片，并标明讲者姓名。
- 报告说明、字幕风险和图表说明必须视觉隔离并标明“非原内容”。
- 广告、产品推广、订阅引导和销售话术不得进入最终正文。
- 五条片尾科技新闻固定命名为“科技五大新闻”，页面不显示视频时间。
- 外部研判与 Agent 判断不得写成作者观点；即使证据不支持，也不得静默修改第一部分。
- Agent 判断必须区分事实、管理层主张、推断和判断，并包含来源日期、置信度、期限、已计价判断、成立条件、量化反证、下行机制、观察姿态和缺失证据。

## 阶段与编排

- 未通过 `import-transcript` 不得分析；原意分析未形成可追溯观点记录不得研究；研究缺少来源和适用条件不得写研判；Agent 判断不完整不得渲染；原意审查或 HTML 验收未通过不得完成。
- 主 Agent 负责状态、观点聚类、证据汇总、最终判断、写作和交付。
- 只在两个以上独立观点主题时并行启动只读研究子 Agent；研究子 Agent 不编辑正文，也不替主 Agent决定最终结论。
- 原意审查第一轮不得读取外部研判结果，只对照字幕包、分析与正文。
- 多个 Agent 不得并发编辑同一份最终 Markdown 或 HTML。

## 工程规则

- 阶段必须幂等，重复运行不得覆盖已验证产物。
- 中间产物写入 `work/{video_id}/`，最终产物写入 `reports/{date}-{video_id}/`。
- 每一步更新 `manifest.json`；失败记录阶段、错误和是否可重试。
- 使用安全参数数组调用外部程序，不拼接未经验证的 shell 命令。
- 修改代码后运行 `python -m unittest discover -s tests -v`。

完整流程与字幕包契约见 `WORKFLOW.md`。
