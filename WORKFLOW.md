# 字幕驱动的观点报告工作流

## 1. 输入契约

本项目只接受 `package_type = video_transcript`、`schema_version = 1` 的字幕包。`package.json` 必须包含视频 ID、来源 URL、标题、作者、发布时间、时长，以及以下带 SHA-256 的文件：

- 原始 ASR：`transcript.jsonl`、`transcript.md`、`transcript.srt`；
- 校订字幕：`transcript.corrected.jsonl`、`transcript.corrected.md`、`transcript.corrected.srt`；
- 审计文件：`corrections.json`、`validation.json`。

导入成功后，文件复制进本项目的 `work/{video_id}/transcript/`。上游字幕包可以移动或离线，不影响当前报告运行。

## 2. 三条证据链

```text
作者内容 → 校订字幕 segment → 原始 ASR → 时间戳与勘误记录
外部研判 → opinion_id → 支持/反对证据 → 来源与日期
Agent 判断 → 前两层 → 一手事实/推断 → 条件、反证、下行机制
```

本项目无法仅凭字幕重新核听音频。若字幕包记录了未解决词项，报告必须显式保留不确定性，不得假称已经回听确认。

## 3. 阶段门

1. `ingest`：字幕包 schema、路径、哈希、质量和校订记录全部通过。
2. `analyze`：形成可追溯的 `video-analysis.json` 与 `opinions.jsonl`。
3. `research`：每条主观观点有独立信源、反方证据和适用条件。
4. `judgment`：形成包含资料日期、置信度、期限、已计价判断、成立条件、量化反证、下行机制、观察姿态和缺失证据的结构化判断。
5. `draft`：报告固定分为三个显式一级部分，第一部分只依据字幕内容。
6. `fidelity_review`：逐段对照校订字幕；不使用外部资料反向改写作者原意。
7. `render`：生成 Markdown、`report-data.json`、`citations.json` 和 HTML。
8. `html_validate`：链接、布局、内容和三层映射检查通过。
9. `complete`：全部产物存在且结构一致后，写入 `state/processed-reports.json`。

任何阶段失败都写入 `work/{video_id}/manifest.json`，下次从失败门恢复；已通过的阶段默认不覆盖。

## 4. 报告内容规则

- 固定顺序：`第一部分｜视频 / 作者内容`、`第二部分｜外部证据研判`、`第三部分｜Agent 综合判断`。
- 第一部分采用内容直述，只能写字幕中明确表达的事实、推理、判断和限定，不混入外部证据或 Agent 推断。
- 讲者个人判断、持仓和交易行动使用“报告标注 · 视频内个人判断”卡片并标明姓名。
- 报告自己的免责声明、字幕不确定性和图表解释必须标成“非原内容”。
- 广告、产品推广、订阅引导和销售话术不得进入最终正文。
- 片尾五条科技新闻固定命名为“科技五大新闻”，页面不显示视频时间。
- 第二部分声明：“本注为基于外部信源形成的独立研判，不代表视频作者观点。”
- 第三部分声明：“本节为 Agent 基于视频内容、既有外部研判和注明日期的公开资料形成的综合判断，不代表视频作者观点，也不构成投资建议。”
- 时间戳只用于定位与覆盖检查，不用于决定主题重要性、报告篇幅或研判权重。

## 5. 信息密度与审查

默认阅读路径为：一页结论图 → 主题卡或对比表 → 证据状态矩阵 → 三层判断总图与紧凑判断卡 → 折叠信源。图表用于降低比较成本，但不能替代事实依据、推理链、限定条件和关键反方视角。

原意审查第一轮只读取导入的字幕包、视频分析和第一部分正文。外部研判与 Agent 判断分别检查来源支撑、事实/推断分层、日期、反证、缺失证据和声明。HTML 验收未通过不得完成运行。

## 6. 输出

中间产物位于 `work/{video_id}/`，最终产物位于 `reports/{date}-{video_id}/`。每次完成至少报告：输入字幕包、勘误数与未决词项、观点数、研判状态分布、Agent 判断主题与观察姿态、审查状态、Markdown/HTML 路径以及尚未解决的限制。
